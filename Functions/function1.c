#include <stdio.h>
#include <math.h>

int main()
{   double n;
    printf("enter a number who square root is to be computed:\n");
    scanf("%lf",&n);
    printf("square root of %lf is %lf",n,sqrt(n));
    return 0;
}
